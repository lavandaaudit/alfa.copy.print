/* catalog-override.js — заглушка (порожній оверрайд).
   Сайт працює зі штатними цінами, поки адмінка не опублікує новий файл.
   Адмінка: download/admin.html → «📥 Опублікувати» → замініть цей файл. */
window.CATALOG_OVERRIDE_LOADED = true;
window.CATALOG_OVERRIDE = null;
